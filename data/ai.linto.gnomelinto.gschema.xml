<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="ai.linto.gnomelinto" path="/ai/linto/gnomelinto/">
    <key name="srt-url" type="s">
      <default>''</default>
      <summary>Streaming URL</summary>
      <description>
        The URL of the LinTO Studio endpoint the local audio stream is sent to.
        This is the currently selected address, taken from the saved address
        list. SRT, for example
        srt://studio.linto.ai:8889?streamid=SESSION_ID,0&amp;mode=caller
        and RTMP, for example rtmp://studio.linto.ai:1935/SESSION_ID/0 are
        supported.
      </description>
    </key>
    <key name="addresses" type="a(ss)">
      <default>[]</default>
      <summary>Saved stream addresses</summary>
      <description>
        The saved stream addresses, each a (label, URL) pair. The user picks
        which one is active; its URL is mirrored to srt-url.
      </description>
    </key>
    <key name="transcription-urls" type="a(ss)">
      <default>[]</default>
      <summary>Public transcription URLs</summary>
      <description>
        Optional public transcription URL for a stream, each a (stream URL,
        transcription URL) pair. Kept separate from the addresses key so an
        upgrade never has to change that key's type. Only streams that have one
        appear here.
      </description>
    </key>
    <key name="control-enabled" type="b">
      <default>false</default>
      <summary>Enable the control server</summary>
      <description>
        Whether the embedded control server (for Stream Controller) is running.
      </description>
    </key>
    <key name="control-password" type="s">
      <default>''</default>
      <summary>Control server password</summary>
      <description>
        The password a Stream Controller client must supply. Generated on first
        use; regenerated only when the user asks for a new one.
      </description>
    </key>
    <key name="debug-enabled" type="b">
      <default>false</default>
      <summary>Enable debug logging</summary>
      <description>
        Whether verbose diagnostic logging to a file is enabled. Off by default;
        used to track down streaming problems.
      </description>
    </key>
    <key name="control-port" type="i">
      <default>4466</default>
      <range min="1" max="65535"/>
      <summary>Control server port</summary>
      <description>
        The TCP port the control server listens on.
      </description>
    </key>
    <key name="srt-latency" type="i">
      <default>500</default>
      <range min="20" max="8000"/>
      <summary>SRT latency</summary>
      <description>
        The SRT send latency in milliseconds. A larger value gives SRT more
        time to retransmit lost packets before the receiver drops them, at the
        cost of extra delay. Ignored when the streaming URL already sets a
        latency parameter, and not used for RTMP.
      </description>
    </key>
    <key name="silence-timeout" type="i">
      <default>120</default>
      <range min="10" max="3600"/>
      <summary>Silence auto-pause timeout</summary>
      <description>
        Seconds of no detected voice before streaming is automatically paused.
        Streaming resumes automatically when voice is detected again. Voice is
        detected by a voice activity detector, so steady background noise does
        not count as speech.
      </description>
    </key>
    <key name="voice-threshold-auto" type="b">
      <default>true</default>
      <summary>Automatic voice level threshold</summary>
      <description>
        When on, the level below which audio counts as silence is derived
        automatically from the recent loudest audio (about 20 dB below it).
        When off, the manual voice-threshold value is used.
      </description>
    </key>
    <key name="voice-threshold" type="i">
      <default>-50</default>
      <range min="-90" max="0"/>
      <summary>Voice level threshold</summary>
      <description>
        Audio below this level (in dBFS) counts as silence and does not keep the
        stream from auto-pausing. Used only when the automatic threshold is off.
        Set it above the background noise shown on the input level meter.
      </description>
    </key>
  </schema>
</schemalist>
